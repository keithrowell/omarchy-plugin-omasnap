## What does this change?

<!-- What it does and why. Link the issue it resolves, if any (Fixes #). -->

## How was this tested?

<!-- `node --test tests/*.test.mjs`, plus anything you checked by hand on a live Omarchy session. -->

## Checklist

- [ ] Tests added/updated for behaviour changes
- [ ] `node --test tests/*.test.mjs` passes locally
- [ ] No hard-coded colours — everything comes from the live theme files
- [ ] No non-Omarchy chrome introduced
